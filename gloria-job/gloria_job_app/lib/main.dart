import 'package:flutter/material.dart';
import 'app/app.dart';

void main() {
  // Memastikan binding Flutter terinisialisasi dengan baik
  WidgetsFlutterBinding.ensureInitialized();

  // Menjalankan aplikasi Gloria Job
  runApp(const GloriaJobApp());
}
